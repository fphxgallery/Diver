# Solana RPC (use a private RPC for production)
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com
NEXT_PUBLIC_WS_URL=wss://api.mainnet-beta.solana.com

# Jupiter API key (https://developers.jup.ag/portal) — required for swap
NEXT_PUBLIC_JUPITER_API_KEY=

# Redis (optional — for position state caching and rebalance job queue)
REDIS_URL=redis://redis:6379

# Server secret for encrypting persisted wallet seeds (required for auto-rebalance after restart)
# Generate with: openssl rand -hex 32
DIVER_SERVER_SECRET=

# Directory for persisted monitor state (default: ./data)
# DIVER_DATA_DIR=/app/data
